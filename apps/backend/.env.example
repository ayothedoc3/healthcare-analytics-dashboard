# Database Configuration (REQUIRED - DO NOT USE THESE VALUES IN PRODUCTION)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=your_db_user_here
DATABASE_PASSWORD=your_strong_password_here_min_16_chars
DATABASE_NAME=med_analytics

# Application
PORT=4000
NODE_ENV=development

# CORS (comma-separated for multiple origins)
CORS_ORIGIN=http://localhost:3000

# Security Notes:
# 1. NEVER commit actual credentials to Git
# 2. Use strong passwords (16+ characters, alphanumeric + special chars)
# 3. In production, set NODE_ENV=production
# 4. For production, use environment-specific secrets management
